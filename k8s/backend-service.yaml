apiVersion: v1
kind: Service
metadata:
  name: arc-backend
  namespace: arc
spec:
  type: NodePort
  selector:
    app: arc-backend
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    nodePort: 30000
