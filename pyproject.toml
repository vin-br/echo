[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "arc"
version = "0.1.0"
description = "ARC project"
readme = "README.md"
requires-python = "==3.14.*"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["backend*", "frontend*", "models*", "modules*"]
exclude = ["legacy*"]

[tool.black]
line-length = 100
